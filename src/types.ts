export type Note = {
    title: string,
    text: string,
    user: string,
    id: string,
}
export type User = {
    username: string,
    password: string,
    id: string,
}
export type Character = {
    name: string,
    race: string,
    occupation: string,
    description: string,
    user: string,
    id: string,
}